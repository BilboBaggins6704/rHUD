RScoreboard = {}
local config = RScoreboard

// Enable Scoreboard.

config.ScoreboardEnabaled = true

// Admin Options.

config.ULX = true -- If you want to use ULX, put this to true.
config.FADMIN = false -- If you want to use FAdmin put this to true.

// Admin Groups.

config.ADMIN = { "Owner", "superadmin", "admin" }

// Scoreboard Options.

config.EnableReconnect = true
config.EnableDisconnect = true

// Scoreboard Colors.

config.MainColor = Color( 24, 24, 24 ) // The main background color of the scoreboard.
config.MainBars = Color( 38, 38, 38, 255 ) // The two grey bars, this changes the colors of them.

config.HostName = Color( 255, 255, 255 ) // Color of your server name at the top.
config.Players = Color( 255, 255, 255 ) // Color of the "PLAYERS:"

config.YourName = Color( 255, 255, 255 ) // Your own name at the top, the color of it.
config.YourJob = Color( 255, 255, 255 ) // Your own job at the top, the color of it.
config.YourMoney = Color( 255, 255, 255 ) // Your own wallet at the top, the color of it.

config.PlayersLine = Color( 255, 255, 255, 255 ) // The color of the line seperating players.
config.MainText = Color( 255, 255, 255 ) // The color of the players information such as kills, name, and deaths.

